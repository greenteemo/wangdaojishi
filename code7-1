#include"stdafx.h"
#include<stdio.h>

long long f[91];

int main() {
	f[1] = 1;
	f[2] = 2;
	for (int i = 3; i <= 90;i++) {
		f[i] = f[i - 1] + f[i - 2];
	}
	int n;
	while (scanf("%d", &n)!=EOF) {
		printf("%lld\n",f[n]);
	}
	return 0;
}
